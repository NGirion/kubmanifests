kind: Pod
apiVersion: v1

metadata:
   name: testpod2
   annotations:
       description: additional info about pod

spec:
  containers:
   - name: c00
     image: ubuntu
     command: ["bin/bash", "-c", "while true; do echo Hello-Satish; sleep 5; done"]
